import {View, Text} from 'react-native';
import React from 'react';
import Card from './Card';

const RecommendedTeams = () => {
  return (
    <Card title="Recommended Teams">
      <Text>RecommendedTeams</Text>
    </Card>
  );
};

export default RecommendedTeams;
