import {Text, StyleSheet, SafeAreaView} from 'react-native';
import React from 'react';

const LeagueList = () => {
  return (
    <SafeAreaView>
      <Text>LeagueList</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default LeagueList;
